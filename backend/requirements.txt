# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
gunicorn==25.1.0
pydantic-settings==2.5.0

# LlamaIndex core + integrations
llama-index-core==0.12.0
llama-index-llms-openai==0.3.0
llama-index-embeddings-openai==0.3.0
llama-index-vector-stores-supabase==0.3.0

# LlamaParse (LlamaCloud)
llama-cloud>=1.0

# Supabase client
supabase==2.9.0

# Reranking (cross-encoder for retrieval precision)
# Primary: ONNX Runtime (fast CPU inference)
onnxruntime>=1.17.0
transformers>=4.40.0
# Export-only (not needed at runtime): pip install optimum[onnxruntime]
# Fallback: FlashRank (PyTorch-based)
flashrank>=0.2.0

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.9

# Rate limiting (slowapi backed by limits, in-process by default)
slowapi==0.1.9
